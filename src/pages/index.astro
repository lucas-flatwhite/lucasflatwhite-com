---
import SnakeExperience from '../components/SnakeExperience.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteProfile } from '../data/site';
---

<BaseLayout
  title={`${siteProfile.name} | ${siteProfile.handle}`}
  description={siteProfile.experienceDescription}
>
  <main class="page-shell experience-page">
    <SnakeExperience />
  </main>
</BaseLayout>
